// Dev default runtime config. The Docker entrypoint regenerates this file
// from the container environment at startup, so no env value is baked into
// the built bundle. All values are empty/false here.
window.__ENV__ = {
  SEED_DEMO: "false",
  SENTRY_DSN: "",
  UMAMI_URL: "",
  UMAMI_WEBSITE_ID: "",
};
